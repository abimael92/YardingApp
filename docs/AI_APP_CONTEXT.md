# AI_APP_CONTEXT (pointer)

The **canonical** AI onboarding document is at the **repository root**:

**[`../AI_APP_CONTEXT.md`](../AI_APP_CONTEXT.md)**

Always read that file before substantive work. This `docs/` copy exists so `.cursorrules` and team runbooks can reference `docs/AI_APP_CONTEXT.md` explicitly.
