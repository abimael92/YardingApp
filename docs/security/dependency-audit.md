# Dependency Audit

## Required Checks
- Run `pnpm audit` in CI.
- Review high/critical advisories before release.

## Automation
- Enable Dependabot for npm/pnpm updates.
- Pin dependency versions (no `latest`).
